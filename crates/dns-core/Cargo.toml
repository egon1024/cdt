[package]
name = "dns-core"
description = "Shared DNS primitives for Cole's DNS Tools"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
